# Coolify API configuration
COOLIFY_API_URL=https://coolify.example.com/api/v1
COOLIFY_API_TOKEN=replace-with-token
PROJECT_UUID=replace-with-project-uuid
# Provide one of the identifiers below depending on your setup
SERVER_UUID=replace-with-server-uuid
# DESTINATION_UUID=replace-with-destination-uuid

# Deployment defaults
BASE_DOMAIN=apps.example.com
ENVIRONMENT_NAME=production

# Factory service configuration
FACTORY_FQDN=factory.apps.example.com
# FACTORY_BASE_URL=https://factory.apps.example.com
PORT=8080
LOG_LEVEL=info
DATA_DIR=/data
